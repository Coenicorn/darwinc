#include <stdio.h>

#include "ecs.h"
#include "world.h"
#include "dyve.h"

int main(void)
{

    // DC_World *g_world = DC_LoadWorldFromFile("empty_16.wld");

    // DC_FreeWorld(g_world);

    printf("Hello World\n");

    return 0;
}